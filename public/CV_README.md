# CV / Resume

Place your CV/Resume PDF file in this folder and name it `cv.pdf`.

The "Download CV" button on the portfolio will link to `/cv.pdf`.

If you want to use a different filename, update the `resumeUrl` in `src/lib/data.config.ts`.
